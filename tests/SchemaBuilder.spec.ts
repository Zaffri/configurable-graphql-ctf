import { expect } from "chai";

import SchemaBuilder from "./../src/core/SchemaBuilder";

describe("SchemaBuilder.ts", () => {
    describe("When configuration has at least 1 enabled modules set", () => {
        xit("setup enabled modules", () => {
            console.log("pending test");
        });
        
        xit("setup default core/shared schema and resolvers", () => {
            console.log("pending test");
        });
        
        xit("stitch core and enabled challenges' schemas & resolvers to build one API schema based on configuration", () => {
            console.log("pending test");
        });
    });
    describe("When configuration has no schema or resolvers set", () => {
        xit("prevent API from starting", async () => {
            console.log("pending test");
        });
    });
});